# 配置版本，请勿修改
version: 4.0.0

# 插件设置
settings:
  # 是否启用调试模式，启用后会在控制台输出详细调试信息
  debug: false
  # 是否启用猫娘模式，启用后Fancy将扮演猫娘，注意某些模型可能会拒绝这个要求
  meow: true
  # 如果支持流式输出但玩家没有设置，是否默认启用流式输出
  streaming: true
  # 补充系统提示词（会自动追加到基础系统提示词后面，不加就留空；注意如果是角色扮演类提示词请关闭Meow模式）
  supplementary_prompt: ""
  # CLI 模式会话的自动过期时间 （以分钟为单位）
  timeout_minutes: 10
  # API 请求超时时间（以秒为单位）
  api_timeout_seconds: 120
  # 上下文窗口剩余警告阈值
  context_window_warning_threshold: 500
  # 上下文窗口大小上限
  context_window_limit: 200000
  # 是否启用匿名错误上报，帮助开发者改进插件
  auto_report: true
  # 是否启用自动更新检查
  check_update: true
  # 是否在管理员进入服务器时提示更新
  op_update_notify: true
  # 是否在检测到新版本时自动下载并安装更新，Paper及下游服务端推荐开启
  auto_upgrade: true
  # 防循环检测的连续相似调用阈值
  anti_loop:
    threshold_count: 4
    similarity_threshold: 0.95
    max_chain_count: 30
  # SMART 模式风险阈值 (0-100)
  smart_risk_threshold: 50
  # 日志保留天数
  log_retention_days: 15
  # YOLO 模式下需要手动确认的风险命令列表
  yolo_risk_commands:
    - op
    - deop
    - stop
    - reload
    - restart
    - kill
    - nbt
    - ban
    - unban
    - plugman
    - plm
    - whitelist
    - lp
    - luckperms
    - data
    - scoreboard
    - tag

# ====== 提供商配置（v4.0+） ======
# fancy = 走 FancyConsole 代理（开箱即用没 key 的兜底方案）
# openai / cloudflare = BYOK 直连（保留现有行为）
provider:
  ai: fancy              # fancy | openai | cloudflare
  search: fancy-tavily   # fancy-metaso | fancy-tavily | metaso | tavily
  jina: fancy            # fancy | none

# FancyConsole 设置
console:
  api_url: "https://api.fancy.baicaizhale.top"
  console_url: "https://console.fancy.baicaizhale.top"

# ====== BYOK 设置（aid/provider 不为 fancy 时使用） ======

# CloudFlare Workers AI 配置
cloudflare:
  cf_key: "cfat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  proxy_url: "https://ptc.fancy.baicaizhale.top"
  model: "@cf/google/gemma-4-26b-a4b-it"
  co-model: "@cf/google/gemma-4-26b-a4b-it"

# OpenAI 兼容端点配置
openai:
  api_url: https://api.deepseek.com/chat/completions
  api_key: "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  model: deepseek-v4-pro
  co-model: deepseek-v4-flash

# Metaso AI 搜索配置（BYOK）
metaso:
  api_token: "mk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  model: pro

# Tavily 搜索 API 配置（BYOK）
tavily:
  api_key: "tvly-dev-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  proxy_url: "https://tavilyproxy.baicaizhale.top"
  max_results: 7

# 公告配置
notice:
  refresh_interval: 5

# 声音反馈配置
sounds:
  enabled: true
  ai_complete: "block.note_block.hat"
  ai_error: "block.note_block.bass"
  cli_enter: "block.note_block.chime"
  cli_exit: "block.note_block.bell"
  user_input: "block.wooden_button.click_on"

# MCP (Model Context Protocol) Client 配置
mcp:
  client:
    enabled: false
    call_timeout: 30
    connect_timeout: 10
    reconnect_interval: 5
    servers: []
